{
  // Automatically restart all the other extensions after loading the direnv
  // environment. This way the other extensions will have the environment as well.
  "direnv.restart.automatic": true,
  "files.associations": {
    ".envrc": "shellscript",
    "renovate.json": "jsonc"
  },
  "fish-lsp.useGlobalExecutable": true,
  "lsp_generic_client.servers": {
    "efm-langserver": {
      "name": "efm-langserver",
      "path": "efm-langserver",
      "args": [
        "-c",
        "efm-langserver.yaml"
      ],
      "documentSelector": [
        {
          "pattern": "**",
          "scheme": "file"
        }
      ]
    }
  },
  "ltex.completionEnabled": true,
  "ltex.enabled": [
    "git-commit",
    "markdown"
  ],
  "nix.enableLanguageServer": true,
  "nix.serverPath": "nixd",
  "nix.serverSettings": {
    "nixd": {
      // We provide values for `config` and `overlays` to avoid using their non-deterministic defaults.
      "nixpkgs": {
        "expr": "import nix/packages.nix"
      }
    }
  },
  "json.schemaDownload.trustedDomains": {
    "https://docs.renovatebot.com/": true
  }
}